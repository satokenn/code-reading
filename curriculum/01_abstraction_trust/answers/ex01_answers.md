# 解答：演習01 — `express` の `app.use()` ミドルウェア連鎖

> ⚠️ **自分の回答を書いてから開いてください。**  
> これらは「正解」ではなく「解答例」です。視点の違いや追加の気づきを比較してください。

---

## 問1 — `next: NextFunction` の役割

`next` は「処理を次のミドルウェアに渡す」関数です。

- `next()` を **呼ぶ** → 次に登録されたミドルウェアが実行される
- `next()` を **呼ばない** → 処理がそこで止まり、以降のミドルウェアは実行されない

ミドルウェア1が `next()` を呼ばなかった場合、ミドルウェア2は実行されません。内部実装を読まなくても、`next` の存在と使用例から「通過するか止めるかを選べる設計」であることが読み取れます。

---

## 問2 — 返り値 `this` が可能にすること

返り値が `this` であることで、**メソッドチェーン（Fluent Interface）** が使えます。

```javascript
app
  .use(middlewareA)
  .use(middlewareB)
  .use(middlewareC);
```

シグネチャの `this` という返り値の型だけで「連続して呼び出せる」ことがわかります。

---

## 問3 — 実行順序の推論

**`GET /` の場合:**
1. パス指定なしのミドルウェア → `req.timestamp = Date.now(); next();` が実行される
2. `/api` のミドルウェアは**スキップ**（パスが `/api` で始まらないため）
3. `app.get('/')` のハンドラが実行される
4. `res.json({ timestamp: ..., isApi: undefined })` が返る

**`GET /api/users` の場合:**
1. パス指定なしのミドルウェア → `req.timestamp = Date.now(); next();` が実行される
2. `req.isApi = true; next();` ← `/api` で始まるのでマッチ
3. `app.get('/api/users')` のハンドラが実行される
4. `res.json({ isApi: true })` が返る

「パス引数があるかどうか」でマッチ条件が変わることが、シグネチャの `path?: PathParams` オプションから推論できます。

---

## 問4 — 登録順序が持つ意味

登録順が実行順を決める設計により、**ミドルウェアの責任の連鎖を明示的に制御**できます。

```javascript
// 推奨される順序
app.use(logging);       // ① まずすべてのアクセスを記録
app.use(authenticate);  // ② 認証チェック（失敗したらここで停止）
app.use(rateLimit);     // ③ 認証済みユーザーにのみレート制限
app.get('/data', ...);  // ④ 実際の処理
```

認証を①に置くと、「記録されていない不正アクセス」が生じるリスクがあります。これはインターフェースの契約（登録順＝実行順）だけから引き出せる設計上の知識です。

---

← 問題に戻る [`ex01_interface_only.md`](../ex01_interface_only.md)
